We are preparing to build context about this codebase to provide to an AI assistant for a review of correctness and
security. Below is a list of files in the repo. Please identify any extensions/globs that can be safely ignored.

<source_tree>
{{ source_tree }}
</source_tree>

The tool we're using accepts a single `--exclude` argument. Please provide the argument to use in a code block,
e.g.:
```
--exclude 'package-lock.json,**/*.dat'
```